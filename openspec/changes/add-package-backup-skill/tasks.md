## 1. Package Backup Skill

- [ ] 1.1 Add focused text contract coverage, then create the
  `package-backup` skill and reference document so archive creation and restore
  tasks follow the `package_backup.sh` behavior contract.
