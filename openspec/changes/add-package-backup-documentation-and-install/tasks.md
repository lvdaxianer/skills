## 1. Package Backup Documentation and Install

- [ ] 1.1 Update the README files and default agent context files so they
  advertise the `package-backup` skill and point agents at its canonical
  reference for archive work.
- [ ] 1.2 Copy the `package-backup` skill into
  `/Users/lvdaxianer/.claude/skills/package-backup` and verify Claude can see
  the installed skill directory.
